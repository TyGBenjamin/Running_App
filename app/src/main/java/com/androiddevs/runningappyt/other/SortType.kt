package com.androiddevs.runningappyt.other

/**
 * Sorting options.
 *
 * @constructor Create instance of [SortType]
 */
enum class SortType {
    DATE, RUNNING_TIME, DISTANCE, AVG_SPEED, CALORIES_BURNED
}
